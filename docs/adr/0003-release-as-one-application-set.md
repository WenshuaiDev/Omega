---
status: accepted
---

# V1 按整套应用发布

Omega V1 为 api、web、console、edge 及所需工具和基础组件形成统一发布版本。发布包记录精确镜像身份和文件校验值，测试与生产复用同一份镜像内容，各环境配置和 Secret 单独管理。

相比各服务独立发布，这一方式减少首版离线现场需要选择、组合和核对的版本，代价是单个应用变更也需生成新的整套发布记录。镜像仍各自构建，统一发布不要求把所有程序打进一个镜像。PostgreSQL 大版本升级不包含在普通应用发布中。
